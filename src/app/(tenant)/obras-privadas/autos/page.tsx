import { ListaAutosGlobal } from "@/components/obras-privadas/listagens-globais";

export const dynamic = "force-dynamic";

/** Listagem global de autos, com filtro de prazos vencidos. */
export default function AutosGlobalPage() {
  return <ListaAutosGlobal />;
}
