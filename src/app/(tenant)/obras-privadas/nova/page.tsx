import { ObraPrivadaForm } from "@/components/obras-privadas/obra-privada-form";

export const dynamic = "force-dynamic";

/** Cadastro de obra privada (RN-PRV-01/02/15). */
export default function NovaObraPrivadaPage() {
  return <ObraPrivadaForm />;
}
