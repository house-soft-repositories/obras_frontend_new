import { AbaDados } from "@/components/obras-privadas/aba-dados";

/** Aba Dados: ficha do proprietario, imovel, localizacao, obra e RTs. */
export default function DadosPage() {
  return <AbaDados />;
}
