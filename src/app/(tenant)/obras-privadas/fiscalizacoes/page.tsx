import { ListaFiscalizacoesGlobal } from "@/components/obras-privadas/listagens-globais";

export const dynamic = "force-dynamic";

/** Listagem global de visitas (cross-obra), para supervisao da equipe. */
export default function FiscalizacoesGlobalPage() {
  return <ListaFiscalizacoesGlobal />;
}
